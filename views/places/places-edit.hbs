<h2>Edit place</h2>

<form action="/places/details/edit/{{place.id}}" method="POST">
    <label for="">
        Name
        <input value="{{place.name}}" name="name" type="text">
    </label>
    </br>

    <label for="">
        Type
        <select name="{{place.type}}">
            <option value="{{place.type}}">Coffee Shop</option>
            <option value="{{place.type}}">BookStore</option>
    
        </select>
    </label>
    </br>

    <button type="submit">Edit place</button>
</form>

<br>
<a href="/">Volver al índice</a>